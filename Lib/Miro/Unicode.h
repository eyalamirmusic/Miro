#pragma once

// Entry header: Unicode character properties — the general category
// of a code point, the \p{..} major classes, the White_Space
// property, and UTF-8 decode / encode.

#include "Unicode/Unicode.h"
