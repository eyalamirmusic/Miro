#pragma once

// Entry header: the raw JSON layer — Miro::Json::Value, parse(),
// print(). No reflection; for struct <-> JSON serialization include
// <Miro/Reflect.h> instead.

#include "JSON/Json.h"
